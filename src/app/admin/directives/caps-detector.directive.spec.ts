import {CapsDetectorDirective} from './caps-detector.directive';

describe('CapsDetectorDirective', () => {
	it('should create an instance', () => {
		const directive = new CapsDetectorDirective();
		expect(directive).toBeTruthy();
	});
});
