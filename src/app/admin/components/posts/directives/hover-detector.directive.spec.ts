import {HoverDetectorDirective} from './hover-detector.directive';

describe('HoverDetectorDirective', () => {
	it('should create an instance', () => {
		const directive = new HoverDetectorDirective();
		expect(directive).toBeTruthy();
	});
});
