import {BlurredModalDirective} from './blurred-modal.directive';

describe('BlurredModalDirective', () => {
	it('should create an instance', () => {
		const directive = new BlurredModalDirective();
		expect(directive).toBeTruthy();
	});
});
